name: 'covid_dw'
version: '1.0.0'
config-version: 2

profile: 'covid_dw'

model-paths: ['models']
seed-paths: ['seeds']
test-paths: ['tests']
analysis-paths: ['analyses']
macro-paths: ['macros']

target-path: 'target'
clean-targets: ['target', 'dbt_packages']

models:
  covid_dw:
    staging:
      +materialized: view
      +schema: STAGING
    marts:
      +materialized: table
      +schema: MARTS
